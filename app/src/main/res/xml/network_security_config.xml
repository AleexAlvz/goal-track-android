<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- IP para o localhost do computador no emulador Android -->
        <domain includeSubdomains="true">10.0.2.2</domain>

        <!-- Adicione aqui o IP da sua máquina na rede local, se for testar em dispositivo físico -->
        <!-- Exemplo: <domain includeSubdomains="true">192.168.1.10</domain> -->
    </domain-config>
</network-security-config>